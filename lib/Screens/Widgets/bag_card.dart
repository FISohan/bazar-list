import 'package:flutter/material.dart';

class BagCard extends StatelessWidget {
  final int bagId;
  const BagCard({Key? key, required this.bagId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
